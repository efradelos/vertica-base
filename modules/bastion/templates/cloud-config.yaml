#cloud-config
write_files:
  - path: /tmp/deploy/key
    permissions: "0600"
    encoding: b64
    content: "${private_install_key}"
  - path: /tmp/deploy/aws.conf
    permissions: "0600"
    encoding: b64
    content: "${aws_conf}"
